# ICNS 실험 플랫폼 기능 명세 (SPEC)

## 1. 문서 목적
본 문서는 ICNS 4단계 실험을 대면 실험실 환경에서 실행하기 위한 웹앱의 단일 기능 명세를 정의한다.

## 2. 기술 스택
- Frontend: Next.js (App Router) + TypeScript
- Backend/API: Next.js Route Handler
- DB: SQLite + Prisma
- 테스트: Playwright e2e
- 배포 형태: 연구실 단일 PC 또는 로컬 네트워크 서버

## 3. 사용자 역할
- **관리자(/admin)**: 세션 생성, 연구 모드 선택, 참가자 코드 발급, 데이터 내보내기
- **참가자(/session/[code])**: 실험 단계 수행

## 4. 연구 모드
- `study1`: 3단계(예측+전이) 기본 생략, 필요 시 관리자 토글로 활성화 가능
- `study2`: 3조건 비교(상호작용 AI/정적 정보/통제)
- `study3`: 2세션 모드(T1, T2 분리 운영)
- `study4`: 보정/중립 사전과제 포함

### 4.1 환경 변수
- `STUDY_MODE=study1|study2|study3|study4`
- `ENABLE_PHASE3=true|false` (study1에서 우선 사용)
- `AI_PROVIDER=mock|openai`
- `RAW_AI_LOGGING=false` (기본)

## 5. 단계 흐름
기본 플로우:
1. 동의
2. 사전측정
3. 1단계 과제
4. 2단계 과제
5. BPNSFS(T1)
6. 3단계 예측 + 전이 수행 (모드/토글에 따라 생략 가능)
7. BPNSFS(T2)
8. 4단계 플랫폼 과제
9. 디브리핑

## 6. 화면 문구(참가자용, 한국어 고정)
- 동의: "본 연구는 문제 해결 과정의 특성을 이해하기 위한 과제 연구입니다. 참여에 동의하시면 다음을 눌러 진행해 주세요."
- 사전측정 안내: "먼저 간단한 사전 질문에 답해 주세요. 정답은 없으며 솔직하게 응답해 주세요."
- 과제 안내: "각 문항을 읽고 가능한 한 정확하게 답해 주세요. 필요 시 제공되는 도구를 사용할 수 있습니다."
- BPNSFS 안내: "다음 문항은 방금 수행한 과제 경험에 대한 질문입니다."
- 4단계 안내: "이제 마지막 과제 세트를 진행합니다. 각 문항을 차례로 제출해 주세요."
- 디브리핑: "참여해 주셔서 감사합니다. 연구 종료 후 상세 설명이 제공됩니다."

## 7. 조건별 AI 인터페이스 정책
- 참가자 화면에는 조건명을 절대 표시하지 않는다.
- 1~3단계에서 조건 분기:
  - 상호작용 AI 조건: 채팅 입력 + 호출 버튼 제공
  - 정적 AI 정보 조건: "해설 보기" 버튼만 제공(읽기 전용)
  - 통제 조건: AI UI 비노출
- 4단계에서는 모든 조건에 동일한 AI UI를 제공한다.

## 8. AI 어댑터 아키텍처
공통 인터페이스:
- `sendMessage({ participant_id, phase, item_id, user_text })`
- 반환값: `{ assistant_text, latency_ms }`

구현체:
1. `MockAIProvider` (기본): 결정적 응답
2. `OpenAIProvider` (옵션): API 키가 없으면 자동 비활성화

## 9. 관리자 기능
- 세션 생성(연구 모드, 시드, 시작/종료 시간)
- 참가자 코드 발급 및 상태 확인(진행 단계, 완료 여부)
- CSV 내보내기(참가자, 응답, 이벤트)
- 원문 로그 ON/OFF 설정

## 10. 무선할당/재현성
- 세션 단위로 `randomization_seed`를 저장
- participant_id 최초 생성 시 조건을 DB에 고정 저장
- 동일 participant_id 재접속 시 기존 조건 재사용

## 11. 개인정보/보안
- 이름/전화번호 등 직접 식별정보는 기본 수집 금지
- 필요한 경우 별도 매핑 파일(오프라인)로 분리 운영
- 원문 프롬프트/응답 저장은 기본 OFF

## 12. 테스트 요구사항
- Playwright e2e 최소 1개:
  - 가짜 참가자 1명이 동의→디브리핑까지 자동 완료
  - 로그 테이블에 핵심 이벤트가 기록되는지 확인
