# ICNS 4단계 실험 플랫폼 작업 지침

## 목표
대면 실험실에서 안정적으로 동작하는 **ICNS 4단계 실험 웹앱**을 구현한다.

## 절대 조건
- 무선할당은 **재현 가능**해야 하며(시드/알고리즘 기록), 동일 참가자는 새로고침해도 조건이 바뀌지 않는다.
- 참가자 화면에는 조건명/집단명(예: 상호작용 AI, 정적 AI 정보, 통제)과 연구 가설 관련 용어를 노출하지 않는다.
- 모든 이벤트 시간은 밀리초(ms) 단위로 기록한다.
- AI 사용 이벤트는 최소 아래 항목을 반드시 기록한다.
  - 버튼 클릭 시점
  - 첫 호출까지 잠복시간
  - 호출 횟수
  - 호출 당시 문항 번호(item_id)
- 데이터는 SQLite에 저장하고, 관리자 화면에서 CSV로 내보낼 수 있어야 한다.
- 연구 1은 **3단계 생략 토글**을 지원해야 한다.

## 개발 규칙
1. 구현 전에 `SPEC.md`와 `DATA_DICTIONARY.md`를 먼저 작성/갱신한다.
2. 구현 후에는 e2e 시나리오(가짜 참가자 1명이 전체 단계를 자동 통과) 테스트를 유지한다.
3. 개인정보는 최소 수집 원칙을 따른다.
4. 프롬프트/AI 원문 로그는 기본값 OFF이며, 관리자 옵션으로만 ON 가능하게 설계한다.

## 품질 체크리스트
- [ ] 연구 모드(study1~study4)별 단계 흐름이 SPEC과 일치한다.
- [ ] 단계별 이벤트 로그가 DATA_DICTIONARY의 필수 필드를 모두 채운다.
- [ ] 4단계 의존 지표(호출횟수/첫호출잠복/무호출 완료비율)를 사후 계산할 수 있다.
